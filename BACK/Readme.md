Backen 

comandos de instalacion, dentro de la carpeta backend,por medio de cd backend

npm init -y 
npm i express
npm i mysql2
npm i cors
npm i nodemon 

estructras basicas de carpetas


dentro de la carpeta backend lleva las siguientes carpetas:
           
                                                            controllers
                                                            dataBase
                                                            router

 crear en la carpte raiz que es la de backend el archivo index.js  


 segundo parcial

 zustand
 customHook
 base de datos 
                query (consultas)  select, insert, update, delete, where,id, pk, fk, relaciones, creacion de database, de tablas, join                                                         

                             